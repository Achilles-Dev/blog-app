<%= bootstrap_form_with(model: @comment, url: user_post_comments_path(post_id: params[:id])) do |form| %>
  <%= form.text_field :text, placeholder: 'Add your comment...', label: 'Comment' %>
  <%= form.submit 'Add Comment', class: 'btn btn-primary text-white mb-3' %>
<% end %>